server:
  port: ${SERVER_PORT:8888}

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_GIT_URI:https://github.com/Qarani-m/aws-configuration}
          default-label: ${CONFIG_GIT_LABEL:dev}
